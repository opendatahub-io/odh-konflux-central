[pytest]
testpaths = unit_tests
pythonpath = .
# Fail fast when a unit test blocks on real sleeps, subprocess, or network (override with @pytest.mark.timeout).
timeout = 10
timeout_method = signal
markers =
    unit: pipeline/CLI unit tests (no live cluster or oc required)
    slow: opt-in tests that may exceed the default per-test timeout
