# integration-tests/rhoai-e2e — Python dependencies for this directory.
## Python deps for integration-tests/rhoai-e2e helpers and Tekton step scripts.
# defusedxml: required for safe JUnit XML parsing (steps/tekton_util.py).
defusedxml>=0.7.1
# PyYAML: local CLI/tests and faster rhoai-e2e-tests-config.yaml loading; Tekton may pip-install if absent.
pyyaml>=6.0
# pytest: pipeline unit tests under integration-tests/rhoai-e2e/unit_tests/ (not cluster BVT/smoke)
pytest>=7.0
# pytest-xdist: parallel unit test runs (make test)
pytest-xdist>=3.0
# pytest-timeout: fail any single unit test that hangs (default 10s in pytest.ini)
pytest-timeout>=2.1
#
# Runtime binaries (install separately): oc (required), tkn (optional; trigger-mode log stream),
# yq (for ITS overrides and YAML→JSON when PyYAML is absent), skopeo (optional, ODH catalog fallback).
